version: '3.8'

services:
  agentfs:
    build: .
    ports:
      - "8080:8080"
      - "8081:8081"
    volumes:
      - .:/data
    environment:
      - AGENTFS_DIRS=/data
    command: ["./agentfs"]