# AgentFS Python Wrapper

This package provides a Python wrapper for the AgentFS native binary.

## Installation

```bash
pip install agentfs
```

## Usage

After installation, the `agentfs` command is available:

```bash
agentfs --version
agentfs config init
agentfs serve
```

The native binary is automatically downloaded from GitHub releases on first use.
