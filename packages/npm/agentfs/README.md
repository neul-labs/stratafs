# AgentFS npm Wrapper

This package provides an npm wrapper for the AgentFS native binary.

## Installation

```bash
npm install -g agentfs
```

## Usage

After installation, the `agentfs` command is available:

```bash
agentfs --version
agentfs config init
agentfs serve
```

The native binary is automatically downloaded from GitHub releases on install.
