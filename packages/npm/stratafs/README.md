# StrataFS npm Wrapper

This package provides an npm wrapper for the StrataFS native binary.

## Installation

```bash
npm install -g stratafs
```

## Usage

After installation, the `stratafs` command is available:

```bash
stratafs --version
stratafs config init
stratafs serve
```

The native binary is automatically downloaded from GitHub releases on install.
